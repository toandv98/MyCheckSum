<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">?android:attr/colorBackground</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="Widget.MaterialComponents.Chip.Filter.Custom" parent="Widget.MaterialComponents.Chip.Filter">
        <item name="chipStrokeWidth">1dp</item>
        <item name="chipStrokeColor">@color/colorPrimary</item>
        <item name="chipBackgroundColor">?android:attr/colorBackground</item>
        <item name="checkedIcon">@drawable/ic_check_circle</item>
        <item name="chipIconSize">@dimen/chip_icon_size</item>
        <item name="chipStartPadding">@dimen/chip_padding</item>
        <item name="chipEndPadding">@dimen/chip_padding</item>
    </style>

    <style name="EditTextLabel" parent="Widget.MaterialComponents.TextView">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/text_size_small</item>
        <item name="android:textStyle">bold</item>
        <item name="android:padding">@dimen/padding_4dp</item>
    </style>

    <style name="TextViewResult" parent="Widget.MaterialComponents.TextView">
        <item name="android:drawablePadding">@dimen/padding_8dp</item>
        <item name="android:drawableEnd">@drawable/ic_content_copy</item>
        <item name="android:background">@drawable/bg_tv_result</item>
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:clickable">true</item>
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>

</resources>
